var data, json = []; $.ajax({ type: "GET", url: "BTUSD.csv", dataType: "text", success: function (t) { const e = t, a = (new DataSet.View).source(e, { type: "csv" }); data = (data = a.rows).reverse(); const n = new DataSet({ state: { start: "13-10-10", end: "19-12-12" } }), r = n.createView("origin").source(data); r.transform({ type: "filter", callback: function (t) { const e = t.Date; return e >= n.state.start && e <= n.state.end } }); const s = new G2.Chart({ container: "container", forceFit: !0, height: 400, padding: [40, 40, 40, 60], animate: !1 }); s.source(r, { Date: { tickCount: 10 }, Open: { type: "linear", tickInterval: 1e3 } }), s.axis("Open", { lable: { textStyle: { fill: "#aaaaaa" } }, grid: null }), s.line().position("Date*Open").color("black").opacity(.85).size(.75), s.render(), s.interact("slider", { container: "slider", padding: [40, 40, 40, 60], onChange: function (t) { const { startValue: e, endValue: a } = t; n.setState("start", e), n.setState("end", a) } }), s.interact("brush", { type: "X", onBrushstart() { }, onBrushend(t) { let e = t.Date.length, a = t.Date[0], n = t.Date[e - 1]; $("#starting_date").text(a), $("#ending_date").text(n) } }) } });